/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  swcMinify: true,
  // Disable powered by header for privacy & performance
  poweredByHeader: false,
};

export default nextConfig;
